// import { getUser, addUser, getUsers,updateUser, getMsgs} from "../controllers/admin.controller";
import { Application } from "express";

export const adminRoutes = (app: Application, io: SocketIO.Server) => {
  // app.route("/api/v1/users").post(addUser);
  // app.route("/api/v1/users").get(getUsers);
  // app.route("/api/v1/user/:sso_id").get(getUser);
  // app.route("/api/v1/user").put(updateUser);
  // app.route("/api/v1/msgs").get(getMsgs);
};